import { InjectionToken } from '@angular/core';

export const USE_LOCAL_STORAGE = new InjectionToken<boolean>('USE_LOCAL_STORAGE');

export const APP_VERSION = new InjectionToken<string>('APP_VERSION');
